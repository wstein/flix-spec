{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://raw.githubusercontent.com/wstein/flix-spec/main/schemas/projection-map.schema.json",
  "title": "Flix Consumer Projection Map Schema",
  "description": "Maps a consumer parser's native node vocabulary onto the canonical TreeKind names in ast/treekind.json. Consumers whose parse trees already carry canonical names need no map.",
  "type": "object",
  "required": [
    "schemaVersion",
    "consumer",
    "mappings"
  ],
  "properties": {
    "schemaVersion": {
      "type": "integer",
      "minimum": 1
    },
    "consumer": {
      "type": "string",
      "minLength": 1,
      "description": "Repository or tool this map belongs to, e.g. tree-sitter-flix."
    },
    "description": {
      "type": "string"
    },
    "mappings": {
      "type": "object",
      "description": "Native node name -> canonical TreeKind name. Every value must exist in ast/treekind.json.",
      "additionalProperties": {
        "type": "string",
        "minLength": 1
      }
    },
    "ignored": {
      "type": "array",
      "description": "Native nodes that are transparent when they wrap at most one child: dropped when empty, replaced by their child when they wrap exactly one. A node may also appear in `mappings`, which is not a contradiction -- elision only fires at arity <= 1, so the two entries describe different positions. Precedence-chain grammars require this: a level like ADDITIVE_EXPR is a pass-through on every expression without a `+`, and a real binary expression when one is present. Anything neither mapped nor ignored is reported as unmapped, which is a gap rather than a decision.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "notes": {
      "type": "object",
      "description": "Free-form per-node rationale, keyed by native node name.",
      "additionalProperties": {
        "type": "string"
      }
    },
    "elide": {
      "type": "array",
      "deprecated": true,
      "description": "DEPRECATED. Canonical TreeKind names this consumer legitimately does not produce, skipped when matching. Only a node with at most one child may be elided -- eliding a branching node would discard structure rather than a wrapper. Superseded by ast/transparency.json, which removes these nodes from fixtures/expected/ before any consumer sees them: doing it once, in the reference's repository, with a reason argued from the reference's own structure, is neutral in a way that doing it per consumer never was. Every entry here that names a kind ast/transparency.json already removes is dead weight and should be deleted. What remains admissible is a consumer-specific extra -- a canonical kind that consumer genuinely cannot produce but that is not transparent in the reference -- and each one is evidence about that consumer, not about the canonical tree.",
      "items": {
        "type": "string",
        "minLength": 1
      }
    },
    "flatten": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Native grouping nodes whose children are spliced into the parent at any arity. Stronger and more dangerous than `ignored`, which only fires at arity <= 1: flattening a node that carries real structure would hide a genuine disagreement. Use only for pure grouping constructs with no counterpart in the reference, and justify each entry in `notes`."
    },
    "recoveryMarkers": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "Native nodes that mark error recovery rather than syntax -- tree-sitter's ERROR node, a Grammar-Kit error element. The consumer-side mirror of ast/transparency.json's `splice` rule, and the reason it must be declared separately from `flatten`: `oracle_conformance` splices these out on both sides so structure is compared modulo recovery, while `recovery_conformance` keeps them on both sides and compares nothing else. A node listed in `flatten` would be removed from both lanes and its recovery shape never measured; one listed here and nowhere else is measured in exactly one lane. Declaring none is a statement in itself -- `recovery_conformance` then reports `not-applicable` with that as the reason, rather than failing a consumer for not modelling recovery at all. Entries may also appear in `mappings`, which is how the recovery lane knows what canonical kind to compare them against; they may not also appear in `flatten` or `ignored`."
    },
    "flattenCanonical": {
      "type": "array",
      "deprecated": true,
      "items": {
        "type": "string"
      },
      "description": "DEPRECATED. Canonical TreeKind names whose children are spliced into the parent at any arity -- the mirror of `flatten`, which does the same on the consumer's side. Needed when the reference groups members the consumer makes direct children: `UsesOrImports.UseOrImportList` holds every use in one node, and `elide` cannot remove it because elision only fires at arity <= 1. Superseded by ast/transparency.json's `splice` rule for the error vocabulary, which is the one case where splicing a canonical node is a fact about the reference rather than about a consumer. Retained for consumer-specific extras; every value must exist in ast/treekind.json."
    }
  },
  "additionalProperties": false
}
