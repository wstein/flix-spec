{
  "schemaVersion": 1,
  "upstream": {
    "repository": "https://github.com/flix/flix",
    "tag": "v0.75.2",
    "commit": "40949531b4d42e5eaf2e4b9997537eaf793c24e7",
    "treeHash": "99213f0a62703908cb337537d57782c28b0ad604",
    "vocabularySources": {
      "main/src/ca/uwaterloo/flix/language/ast/SyntaxTree.scala": "e90b106616ad8a6f59970f02f9c85051c9e9aaf4",
      "main/src/ca/uwaterloo/flix/language/ast/TokenKind.scala": "7749491631d8b048c8b84cfe13b951c610baf7b3",
      "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala": "5ca8746ebd3f4ee780f8aad49d244b292b405e03",
      "main/src/ca/uwaterloo/flix/language/phase/Lexer.scala": "a87b9c58a8766823ebe970c11b070c67c959ff57"
    },
    "vocabularySourcesNote": "Blob SHAs at the pinned commit for the files that define the tree and token vocabularies and the parser's behaviour. tools/oracle/check-drift.sh compares these against upstream to detect that the oracle has moved in a way that matters -- a release-tag comparison alone reports 'current' while master has already changed these."
  },
  "oracleArtifact": {
    "kind": "release-asset",
    "url": "https://github.com/flix/flix/releases/download/v0.75.2/flix.jar",
    "sha256": "a2697d875725a0dde6e793b8d54cb220e86167a6d49ec5f0ccb0832966c8c15a",
    "sizeBytes": 33931077,
    "jarEntries": 16848,
    "attestation": "digest-only -- release asset without provenance attestation or build commit stamp",
    "note": "Pinned by digest, not rebuilt. The digest above is the contract; nothing here re-derives it. Historical evidence that a same-commit rebuild need not match a release asset remains in docs/phase0-spike.md section 'Q2'."
  },
  "buildProvenance": {
    "note": "Provenance metadata only -- flix-spec's own build does not need to match this to consume the pinned artifact above (implementation plan section 3.2).",
    "buildTool": "mill",
    "buildToolVersion": "1.1.5",
    "command": "./mill flix.assembly",
    "outputPath": "out/flix/assembly.dest/out.jar",
    "scalaVersion": "2.13.18",
    "jdkTarget": 21,
    "loadBearingScalacOptions": [
      "-Xfatal-warnings",
      "-Ypatmat-exhaust-depth 400",
      "-release 21",
      "-opt:inline:ca.uwaterloo.**",
      "-Xmixin-force-forwarders:false",
      "-Xsource:3",
      "-Ytasty-reader"
    ]
  },
  "entryPoint": {
    "description": "Phase pipeline, driven directly: Reader.run(List(Input.RealFile), AvailableClasses.empty) -> Lexer.run -> Parser2.run, with an implicit Flix. Chosen over the public API because it parses exactly the given inputs.",
    "classes": [
      "ca.uwaterloo.flix.api.Flix",
      "ca.uwaterloo.flix.language.phase.Reader",
      "ca.uwaterloo.flix.language.phase.Lexer",
      "ca.uwaterloo.flix.language.phase.Parser2",
      "ca.uwaterloo.flix.language.ast.SyntaxTree"
    ],
    "alternativeRejected": "new Flix() -> addVirtualPath -> check() -> getParsedAst() is public and simpler, but getParsedAst() returns the CST for the entire compilation including the standard library (~1.03M projected nodes, 143 of 192 kinds for a one-line fixture), so every fixture expectation would embed the stdlib. Retained as the route for the corpus-wide reachability run, where that breadth is the point."
  },
  "libraryLevel": {
    "fixtures": "Options.TestWithLibNix",
    "corpus": "Options.TestWithLibAll"
  },
  "classpath": {
    "requirement": "system-classpath",
    "note": "Must be loaded on the system classpath. URLClassLoader isolation hides dev.flix.runtime.Global and returns empty trees."
  },
  "treeKindCount": 191,
  "treeKindDigest": "10faeb18f4e5acdbd98822304a05cba09daf2f92165faaf525a32d0ac975f69b",
  "tokenKindCount": 158,
  "tokenKindDigest": "b88de74fc050c7f424b016921f588920e7571c42e49d08eaeeee7dd374bbc305"
}
