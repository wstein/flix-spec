{
  "schemaVersion": 1,
  "upstreamCommit": "f2d4678c20bff1242f4cad5e23144db91027b762",
  "note": "HAND-MAINTAINED. The curated input to fixture normalisation: every entry names a canonical TreeKind that ProjectionExtractor removes when it writes fixtures/expected/, and that fixtures/raw/ still carries verbatim. Two rules exist. `elide` is for nodes that contribute exactly one edge and no leaf content -- across the whole suite they never hold more than one child and never hold a token, so their name can never be what groups siblings or what gives a token its role. `splice` is for the error vocabulary, which is not syntax at all; splicing it out is what makes the main conformance lane measure structure *modulo* recovery, and the recovery lane is where its shape is measured instead. Every reason below is an argument about the reference's own structure. A reason of the form 'consumer X does not produce one' is inadmissible and must be rejected in review: this repository instruments a single consumer, so no automated check can distinguish a neutral rule from one shaped by that consumer, and the reasons are the only thing that can. Line numbers refer to upstreamCommit, so a pin bump fails until every citation has been re-read against the new source. Deliberately absent: UnclosedMark. It is a placeholder the parser overwrites and can appear in no tree from any input, so it is argued in ast/unattachable.json instead -- a normalisation rule for a node that never occurs would be unfalsifiable, and claiming it is 'measured in the recovery lane' would be false.",
  "treeKinds": [
    {
      "name": "Argument",
      "rule": "elide",
      "reason": "argument() opens a mark, parses one expression, and closes Argument only on the branch where no `=` follows -- the named branch closes ArgumentNamed instead. The single expression is therefore the whole content, and the position in the enclosing ArgumentList is what makes it an argument.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:1857",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:2101"
      ]
    },
    {
      "name": "Doc",
      "rule": "elide",
      "reason": "docComment() opens a mark and immediately closes it as Doc, relying on open() having already consumed any leading comment run into a CommentList. So Doc holds either nothing, when the declaration is undocumented, or exactly that one CommentList, which carries the comment tokens; Doc itself never holds a token.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:1386"
      ]
    },
    {
      "name": "ErrorTree",
      "rule": "splice",
      "recoveryMarker": true,
      "reason": "Not syntax. closeWithError and advanceWithError close a mark as ErrorTree while recording a diagnostic; the latter first consumes the unexpected token. The node records where recovery happened. Its children are the salvaged material and belong to the enclosing production.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:332",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:344"
      ]
    },
    {
      "name": "Expr.DebugInterpolator",
      "rule": "elide",
      "reason": "The debug-interpolation production opens a mark, parses one expression and closes DebugInterpolator around it with no delimiter tokens of its own; the surrounding interpolation tokens belong to the enclosing string expression.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3223"
      ]
    },
    {
      "name": "Expr.HoleVariable",
      "rule": "elide",
      "reason": "The hole-variable production closes around a single name node; the `?` marker is lexed into the name token itself rather than kept as a separate child, so the node adds an edge and nothing else.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:1990"
      ]
    },
    {
      "name": "Expr.StructPutRHS",
      "rule": "elide",
      "reason": "The struct-put production opens a second mark after the `=` token, which it has already eaten, and closes StructPutRHS around the single right-hand expression. The operator stays a child of the enclosing Expr.StructPut, so this node holds one expression and nothing else.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:1551"
      ]
    },
    {
      "name": "Expr.UnsafeAsEffFragment",
      "rule": "elide",
      "reason": "unsafeExpr()'s own shape comment shows the fragment holding exactly one Type.Type, and the production closes it immediately after that single type; the `as` keyword is eaten before the mark is opened.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:2666",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:2681"
      ]
    },
    {
      "name": "OperatorError",
      "rule": "splice",
      "recoveryMarker": true,
      "reason": "Not syntax, and not even text. It is closed over a zero-width mark opened and closed at the same position when a binary operator is missing, so it holds no children and no tokens at all; it exists to give Weeder2 a place to raise MissingBinaryOperator later.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:1475"
      ]
    },
    {
      "name": "Pattern.Variable",
      "rule": "elide",
      "reason": "variablePat() opens a mark, calls nameUnqualified once and closes; the name becomes an Ident node, so Pattern.Variable holds exactly that one node. An Ident directly under a pattern position already is the variable case, so the wrapper distinguishes nothing.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3314"
      ]
    },
    {
      "name": "Predicate.Body",
      "rule": "elide",
      "reason": "Every close site wraps exactly one already-closed predicate node -- atom, functional or guard -- with the leading `not`/`fix` qualifiers eaten into that inner node rather than kept here, so the body node contributes one edge and no leaf content.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3907",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3910",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3913"
      ]
    },
    {
      "name": "Predicate.LatticeTerm",
      "rule": "elide",
      "reason": "The lattice-term productions eat the `;` separator before opening the mark and close around a single expression, pattern or type, so the node never holds the separator and never holds more than the one term.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3796",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3878",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3895",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:4006"
      ]
    },
    {
      "name": "TrailingDot",
      "rule": "splice",
      "recoveryMarker": true,
      "reason": "Not syntax. The lexer leaves a dangling `.` that starts no selection, and the parser wraps that one token in a TrailingDot so recovery can continue past it. The token itself is real source text and stays; the marker records that recovery consumed it.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:777",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:802"
      ]
    },
    {
      "name": "Type.Argument",
      "rule": "elide",
      "reason": "The type-argument production closes around a single Type.Type and never holds the surrounding brackets or commas, which belong to the enclosing Type.ArgumentList.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3499"
      ]
    },
    {
      "name": "Type.Effect",
      "rule": "elide",
      "reason": "Both close sites wrap a single effect type after its separator has already been eaten: a backslash on the normal branch, or a slash on the recovery branch that emits ExpectedBackslashGotSlash. The node carries one type node and no token of its own.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3401",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3406"
      ]
    },
    {
      "name": "Type.Type",
      "rule": "elide",
      "reason": "The type productions re-close a single already-closed type node as Type.Type at every level of the precedence climb, using openBefore on the node they just built. It is the type equivalent of a parenthesis-free grouping level: one child, never a token, and never more than one.",
      "citations": [
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3418",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3436",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3445",
        "main/src/ca/uwaterloo/flix/language/phase/Parser2.scala:3610"
      ]
    }
  ]
}
